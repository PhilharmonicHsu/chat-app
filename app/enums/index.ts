export enum ConnectionState {
    DISCONNECTED = "DISCONNECTED",
    CONNECTING = "CONNECTING",
    CONNECTED = "CONNECTED",
    RECONNECTING = "RECONNECTING",
}

export enum Mode {
    CHAT = 'chat',
    MEETING = 'meeting',
    PREPARING = 'preparing'
}